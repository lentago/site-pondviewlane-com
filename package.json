{
  "name": "site-pondviewlane-com",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "compose": "node scripts/compose-content.mjs",
    "predev": "npm run sync && npm run compose",
    "dev": "astro dev",
    "build": "npm run build:pondview && npm run build:essexcrossing",
    "build:pondview": "SITE=pondview npm run sync && SITE=pondview npm run compose && SITE=pondview astro build",
    "build:essexcrossing": "SITE=essexcrossing npm run sync && SITE=essexcrossing npm run compose && SITE=essexcrossing astro build",
    "postbuild": "npm run check",
    "check": "node scripts/check-content.mjs",
    "preview": "astro preview",
    "test:a11y": "pa11y-ci --config pa11yci.config.json"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@astrojs/starlight": "^0.36.0",
    "astro": "^5.6.0",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "pa11y-ci": "^4.1.1"
  }
}
